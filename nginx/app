server {
       listen 4000;
       listen [::]:4000;

       server_name _;

       root /app/dist/browser;
       index index.html;

       location / {
               try_files $uri $uri/ /index.html =404;
               expires 5m;
       }
}
